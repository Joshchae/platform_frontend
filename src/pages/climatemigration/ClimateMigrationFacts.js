import React, { useState } from 'react'
import './ClimateMigration.css'
import AnimatedFactCards from '../../components/cards/AnimatedFactCards'

function ClimateMigrationFacts() {
    // const postCards = AnimatedFactCards()
    // const [selectedCard, setSelectedCard] = useState(null);
    return (
        <div>
            Fact Cards shall be displayed here  
        </div>
    )
}

export default ClimateMigrationFacts;


// {postCards.map((post) => (
//     {/* <Marker key={post.id} longitude={post.location.longitude} latitude={post.location.latitude} offsetLeft={-20} offsetTop={-10}>
//       <div>
//         <button className="marker-btn" onClick={e => {e.preventDefault(); setSelectedPost(post)}}>
//           <GiPositionMarker />Click here
//         </button>
//       </div>
//     </Marker> */}
//   ))}



